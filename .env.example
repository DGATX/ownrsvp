# =============================================================================
# OwnRSVP Configuration
# =============================================================================
#
# FOR DOCKER USERS: This file is OPTIONAL!
# ----------------------------------------
# Just run: docker compose up -d
#
# - AUTH_SECRET is auto-generated if not provided
# - First registered user becomes admin automatically
# - SMTP can be configured via Admin UI after setup
#
# Only create a .env file if you need custom settings (production domain,
# external database, or pre-configured SMTP).
#
# =============================================================================

# Database Configuration
# ----------------------
# For SQLite (local development without Docker):
DATABASE_URL="file:./dev.db"

# For PostgreSQL (Docker uses this automatically):
# DATABASE_URL="postgresql://postgres:ownrsvp_secure_2024@db:5432/rsvp_db?schema=public"

# Authentication
# --------------
# Leave empty or set to "auto" for auto-generation (recommended for Docker)
# Or generate your own: openssl rand -base64 32
AUTH_SECRET=""

# URLs - change these for production deployments
AUTH_URL="http://localhost:3000"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Email Configuration (OPTIONAL)
# ------------------------------
# Configure via Admin UI after setup, or set here for pre-configuration.
#
# For Gmail: Use an App Password (https://myaccount.google.com/apppasswords)
# SMTP_HOST="smtp.gmail.com"
# SMTP_PORT="587"
# SMTP_USER="your-email@gmail.com"
# SMTP_PASSWORD="your-app-password"
# SMTP_FROM="Events <noreply@your-domain.com>"

# Cron Secret (OPTIONAL)
# ----------------------
# Required only if using automated reminders via external cron service
# Generate: openssl rand -hex 16
CRON_SECRET=""

# PostgreSQL Password (Docker only)
# ---------------------------------
# Change this in production!
POSTGRES_PASSWORD="ownrsvp_secure_2024"
